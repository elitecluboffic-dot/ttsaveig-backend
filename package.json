{
  "name": "ttsaveig-backend",
  "version": "1.0.0",
  "description": "Backend ttsaveig (TikTok & Instagram downloader) — versi Cloudflare Workers.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
